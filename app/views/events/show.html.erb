<h1>hello show</h1>

<h2><%= @event.name %></h2>

<div class="col-xs-12 col-md-6 col-md-offset-3">
  <div class="booking-form">
    <%= simple_form_for [@event, @participation] do |f| %>
      <%= f.input :first_name, label: "Votre nom", placeholder: 'Doe' %>
    <%= f.input :last_name, label: "Votre prénom", placeholder: 'Doe' %>
      <%= f.button :submit, 'Envoyer', class: "btn-plusone_regular" %>
    <% end %>
  </div>
</div>


