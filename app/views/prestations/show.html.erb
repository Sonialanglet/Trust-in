<div class='container'>
  <div class="row">

      <h2 class="name-show"><%= @prestation.user.first_name%> <%= @prestation.user.last_name%> </h2>
        <div class="col-xs-12 col-md-6 col-md-offset-3">
          <% if @prestation.user.facebook_picture_url %>
            <% avatar_url = @prestation.user.facebook_picture_url %>
          <% elsif @prestation.user.photo.present? %>
            <% avatar_url = cl_image_path(@prestation.user.photo, crop: :thumb, graviti: :face) %>
          <% else %>
            <% avatar_url = "profile_icon.png" %>
          <% end %>

          <div class="photo-show">
           <%= image_tag avatar_url, height: 150, width: 150, crop: :thumb %>
          </div>

          <div class="card-white-bottom">
            <div class="card-details">
              <h2 id="prestation_job"><%= @prestation.category.name %></h2>
              <p id="prestation_price"><%= @prestation.price %> €</p>
            </div>
            <p id="prestation_description"><%=  @prestation.description %> </p>
            <% if policy(@prestation).destroy? %>
              <% if current_user == @prestation.user %>
                <%= link_to prestation_path(@prestation),
                      method: :delete,
                      data: { confirm: "Are you sure?" } do %>
                  <p class="cancel-btn">Supprimer</p>
              <% end %>
              <% end %>
            <% end %>
          </div>

      </div>
  </div>
</div>


